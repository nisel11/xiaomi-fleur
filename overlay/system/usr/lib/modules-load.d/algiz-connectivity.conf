# Select vendor dlkm to load on boot as per init .rc files to e.g. prepare connectivity modules,
# rest loaded via /usr/libexec/lxc-android-config/device-hacks once Halium LXC container userspace is ready
conninfra
connfem
fpsgo
