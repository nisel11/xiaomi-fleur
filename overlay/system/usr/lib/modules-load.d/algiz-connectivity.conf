# Select vendor dlkm to load on boot as per init .rc files to e.g. prepare connectivity modules,
# rest loaded via enable-mtk-connectivity.service once Halium LXC container userspace is ready
conninfra
connfem
fpsgo
